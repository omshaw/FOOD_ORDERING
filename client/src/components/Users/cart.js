import React from 'react';
import { Link, useNavigate } from 'react-router-dom';

const Cart = () => {
    return (
        <div style={{marginBottom:'40%'}}>
        <h1>    Cart is Empty ! </h1>
        </div>
    );
}

export default Cart;